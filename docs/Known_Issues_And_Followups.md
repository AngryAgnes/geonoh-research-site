# Known Issues And Followups

- No implementation yet — planning artifacts only.
- Supabase manual setup required later (project, DB, RLS, storage buckets).
- Google OAuth manual setup required later (Supabase/OAuth credentials and redirect URIs).
- Alpha Vantage API key optional — mock fallback will be used if missing.
- Deployment not configured yet; Vercel config will be added during T0023.
